import type { Config } from "@react-router/dev/config";

export default {
    // Config options...
    // Server-side rendering (SSR) is enabled by default.
    // If you want to disable it (SPA mode), set ssr: false.
    ssr: true,
} satisfies Config;
